coverage:
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: auto

flags:
  backend:
    paths:
      - backend/
  frontend:
    paths:
      - frontend/
