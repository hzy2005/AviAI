# AI 功能集成贡献说明

姓名：何周屹  
学号：312190419  
日期：2026-04-21

## 我完成的工作

### 1. AI 功能
- 功能类型：社区发帖场景 AI 文案辅助（看图生成 / 文案润色）
- 使用模型：DeepSeek 文本模型 + 视觉模型（多模态），并结合本地鸟类识别与图像特征分析

### 2. 实现内容
- [x] 后端 API
- [ ] 前端调用
- [x] 错误处理

具体工作说明：
- 新增并完善 `POST /api/v1/posts/ai-copywriting`，支持：
  - `mode=generate`：用户仅上传图片时自动生成分享文案
  - `mode=polish`：用户已有文案时进行润色（含 lite/enhanced 双版本）
- 新增 `POST /api/v1/posts/upload-image`，统一前端先上传图片再调用 AI 接口，保证模型可访问图片数据
- 在生成链路中加入“鸟类识别结果 + 图像视觉事实（颜色/位置/动作等）”作为提示词上下文，提升文案贴图准确性
- 增加输出质量校验与一次重试机制，减少空文案、模板化文案与低质量输出
- 增加可观测性字段（`mode/model/elapsedMs/retryCount/fallback/params` 等），便于排查效果与性能问题
- 修复图片 URL 可访问性问题（避免 `__pageframe__` 相对路径导致模型无法看图）
- 修复并统一中文错误提示，解决编码乱码问题
- 补充 AI 接口文档说明，明确调用顺序与返回结构

## PR 链接
- PR #29: https://github.com/hzy2005/AviAI/pull/29

## 心得体会
本次 AI 集成最大的经验是：仅“接入模型”并不等于“效果可用”。要让文案真正贴图，必须把上传链路、图像可访问性、视觉上下文约束、质量校验和可观测性一起打通。后续我会继续迭代提示词与评测样本，提升文案自然度与稳定性。
