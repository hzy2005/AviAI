# 后端开发贡献说明

姓名：何周屹  
学号：2312190419  
日期：2026-04-14

## 我完成的工作

### API 实现
- [√] 用户认证 API（注册 / 登录 / 退出 / 当前用户）
- [√] 业务资源 1 CRUD：Posts（创建、列表、详情、更新、删除）+ 点赞 + 评论
- [√] 业务资源 2 CRUD：Bird Records（识别创建、列表、详情、更新、删除）
- [√] 统一错误响应（统一 `code/msg/message/data` 结构，含 401/403/404/409/500）

### 数据库
- [√] 数据模型定义（`user` / `bird_record` / `post` / `comment` / `like`）
- [√] ORM 配置（SQLAlchemy）
- [√] 数据库迁移脚本（Alembic）
  - `20260324_0001_create_users_table.py`
  - `20260412_0002_create_bird_records_table.py`
  - `20260412_0003_create_posts_comments_likes_tables.py`

### 部署
- [ ] Dockerfile 编写
- [ ] docker-compose.yml 配置
- [√] 本地联调验证（Uvicorn 启动 + Apifox 接口调试 + 单元测试通过）

## 关键实现说明

1. 联调契约对齐  
完成 Auth / Birds / Posts 返回字段与状态码对齐，特别是前端实际依赖字段（如 `token`、帖子作者信息、识别记录字段）。

2. 持久化迁移  
将 auth、birds、posts、comments、likes 从 mock 形态迁移到 MySQL，保证刷新后数据不丢失。

3. 上传与静态资源  
实现识别图片上传落盘，并挂载 `/uploads` 静态目录，解决前端图片 404 问题。

4. 识别能力接入  
在识鸟流程中接入模型推理（含兜底逻辑），并增加上传文件格式合法性校验。

5. 测试补齐  
补充并维护后端测试，覆盖登录-识别-记录链路及社区 CRUD + like + comment 关键路径。

## PR 链接

- PR #X: https://github.com/hzy2005/AviAI/pull/26

## 遇到的问题和解决

1. 问题：前端加载 `/uploads/*.jpg` 返回 404。  
解决：新增上传文件落盘逻辑并在 FastAPI 中挂载 `/uploads` 静态目录。

2. 问题：联调时接口状态码与前端/文档预期不完全一致。  
解决：统一梳理 Auth、Birds、Posts 契约，补充错误码分支（401/403/404/409）。

3. 问题：参数校验报错格式与业务响应不统一。  
解决：新增全局异常处理，将校验异常统一映射到业务错误结构。

## 心得体会

本次后端开发重点在“从可跑到可联调、可验收”的收口：不仅要实现接口功能，还要保证返回契约稳定、错误码可预测、数据可持久化。通过这次迭代，我对 API 契约管理、数据库迁移和联调排障流程有了更完整的工程化认识。
